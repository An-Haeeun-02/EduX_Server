spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edux
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # create / update / validate / none 중 선택
    show-sql: true         # 콘솔에 SQL 출력
    database-platform: org.hibernate.dialect.MySQL8Dialect

  data:
    mongodb:
      uri:  mongodb+srv://jeonhy1018:root@edux.rawuyuk.mongodb.net/EduX?retryWrites=true&w=majority&appName=eduX